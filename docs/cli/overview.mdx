---
title: "Overview"
description: "Every memloom command, and how the CLI relates to the daemon"
icon: "terminal"
---

## Overview

```bash
npm install -g memloom     # Node 20 or later
memloom init               # creates ~/.memloom, starts the daemon
```

The CLI is a thin client. Every command talks to the daemon over HTTP on `127.0.0.1:4319` and
starts it if it is not running, so the CLI, agents and the viewer share one store with no lock
conflicts.

<CardGroup cols={3}>
  <Card title="Try without installing" icon="bolt">
    `npx memloom@latest <command>`. Pin `@latest` so npx does not reuse a stale copy.
  </Card>
  <Card title="Self-documenting" icon="circle-question">
    `memloom help` lists everything. `memloom help save` explains one.
  </Card>
  <Card title="Configure once" icon="sliders" href="/guides/configuration">
    `~/.memloom/config.env`. Set `OPENROUTER_API_KEY` and restart for cloud mode.
  </Card>
</CardGroup>

***

## Commands

| Command | Does | Page |
| --- | --- | --- |
| `init` `serve` `stop` `ui` | Set up, run, stop the daemon, open the viewer | [Daemon](/cli/daemon) |
| `save` `recall` `update` `history` | Write and read memories | [Memories](/cli/memories) |
| `context add` `list` `remove` | Ingest files and folders | [Context](/cli/context) |
| `index` `reembed` `auto-index` `schema` | Build and manage the graph | [Graph](/cli/graph) |
| `conflicts` `conflicts auto` | Settle contradictions | [Conflicts](/cli/conflicts) |
| `reconcile` and its subcommands | The pass over your own store | [Reconciliation](/cli/reconcile) |
| `import sessions` `import agent-memory` | Bring in what agents already know | [Import](/cli/import) |
| `notion` | The Notion connector | [Notion](/cli/notion) |

***

## Inspecting the store directly

On the embedded tier the daemon also speaks the Postgres wire protocol. Point Drizzle Studio, psql
or any client at:

```
postgresql://postgres@127.0.0.1:54329/postgres
```

`MEMLOOM_PG_PORT` moves it, and `memloom serve` prints the live one. With `MEMLOOM_PG_URL` set
there is no bridge, so connect to your server directly.

<Warning>
  PGLite takes one connection at a time. While a database client is attached, memory endpoints
  answer 503. Disconnect to resume.
</Warning>
