{
  "name": "memloom-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Throwaway Phase 0 spike — validates PGLite can run the memloom engine's load-bearing DB primitives. Delete after.",
  "scripts": {
    "spike": "node spike.mjs"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.4.0"
  }
}
