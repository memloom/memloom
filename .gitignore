node_modules/
dist/
*.log
.env
.env.local
.DS_Store

# embedded viewer bundle (copied from apps/viewer/dist at build time)
packages/cli/viewer/
.gstack/
docs-review/

# local session notes, not part of the project
TODOS.md
TESTING-claude-code-import.md

# locally-installed agent skills (fetched via `npx skills add`), vendored
# per-machine, not part of the project source
.agents/
.claude/skills/
skills-lock.json
